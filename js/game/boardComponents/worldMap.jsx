define(['react'], function(React) {

    var WorldMap = React.createClass({
        render: function(){
            return(
                <div className = "world-map" />
            )
        }
    })

    return WorldMap;
});
